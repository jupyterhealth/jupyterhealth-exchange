info:
  name: Patient Auth Code
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/patients/{{PATIENT_ID}}/invitation_link?application_id={{CLIENT_APP_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
